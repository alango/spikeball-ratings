# Postgres connection string (Neon via Vercel Marketplace). Required at runtime.
DATABASE_URL=postgres://user:password@host/db?sslmode=require

# Shared admin PIN gating the organizer-only endpoints (roster + game CRUD).
# Sent by the admin UI as the `x-admin-pin` header. Required for admin actions.
ADMIN_PIN=change-me
